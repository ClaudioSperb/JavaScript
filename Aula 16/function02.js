function soma(n1=0, n2=0){ //Função que soma dois números, com valores padrão 0
    return n1 + n2
}
let res = soma(2) //Chama a função com apenas um argumento
console.log(`A soma dos valores é ${res}`) //Mostra o resultado no console